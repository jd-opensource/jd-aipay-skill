# 客户端 SDK 指引

## 触发

用户询问 App SDK、iOS、Android、眼镜端、声纹支付、SDK 包、客户端集成、插件、桥接层等。

## 口径

客户端 SDK 接入需要以京东 AI付商务或技术支持提供的 SDK 包和正式方案为准。默认先推进服务端接入、沙箱联调、产品开通指引、生产配置替换和上线验证；用户拿到客户端 SDK 方案后，可继续协助梳理服务端配合项和联调排查。

## 避免

- 不承诺自动下载或集成 SDK 包。
- 不使用旧 SDK 文档作为当前正式方案。
- 不把眼镜端或 App 端代码接入作为默认主流程。
