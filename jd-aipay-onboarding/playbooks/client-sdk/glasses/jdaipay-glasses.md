# 眼镜支付主流程 — JD AI Pay (京东外场AI付 · 眼镜端)

> ⚠️ **预留 / 建设中（TODO）**。眼镜支付（眼镜付 / glasses pay）的 SDK 接入能力尚未上线，本文件
> 为占位主流程。`SKILL.md` 命中「眼镜支付 / 眼镜付 / glasses pay」时路由到这里。

## 当前状态

眼镜端 AI 付 SDK 与接入手册**尚在建设中**。收到眼镜支付请求时，**不要臆造 SDK 类名、依赖或调用**，
按下面的方式向用户说明现状并引导后续对接：

用中文告知用户，例如：

> 「京东 AI 付的**眼镜支付**接入能力正在建设中，SDK 与接入手册尚未正式提供。目前可以先接入**标品支付**
> （收银台 / 声纹支付），眼镜端能力上线后我再帮你补齐。如果你已经拿到眼镜端 SDK，请提供 SDK 文件与接入
> 手册，我按实际内容接入。」

若用户**同时**提到标品与眼镜，先按 `../standard/jdaipay-integration.md` 完成标品接入，眼镜部分说明现状。

## 上线后的主流程（待补充，结构对齐标品）

眼镜端 SDK 就绪后，此文件补成与标品一致的 5 步流程：

1. **Detect** — 运行 `<skill-dir>/scripts/client-sdk-scripts/detect_env.sh` 检测平台（android / ios / both / none）。
2. **Locate** — 搜索眼镜端 SDK 二进制（依赖/框架，具体名称待手册确定）。
3. **Integrate** — 按平台读详细步骤：
   - Android → `android.md`
   - iOS → `ios.md`
4. **Smoke-test** — 构建 + 初始化冒烟测试，捕获初始化日志。
5. **Report** — 中文报告：已完成文件 / 注意事项 / 测试结果 / 后续待办。

眼镜端相较标品的差异（设备绑定、生物特征鉴权、收银台唤起方式）在两个平台的详细文件中展开。
共享的故障排查见 `../notes.md`。
